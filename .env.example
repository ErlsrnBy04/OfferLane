# Copy this file to .env and adjust as needed.
#
# The app uses a local SQLite file as its database. The path is relative to
# the project root, not the prisma/ directory.
DATABASE_URL="file:./dev.db"
